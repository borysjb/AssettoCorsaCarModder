#include <bits/stdc++.h>
#include <curses.h>
#include <panel.h>

class Engine {
    vector<std::string> engineSettings();

    public:
    Engine(const std::filesystem::path &carpath, const std::string &carname, WINDOW *win) {

    }



}